# Codebase Audit & Fix Plan

## Codebase Rating: 7.5/10

### Strengths
- Excellent architectural separation (8 split-context domains, singleton AI queue, repository pattern)
- 112 passing tests across 8 suites
- Strict TypeScript (`strict: true`) enabled
- React Compiler integration with Babel
- Mostly correct Reanimated SharedValue patterns
- Proper `useWindowDimensions()` usage (no module-level `Dimensions.get`)
- No active usage of deprecated `useStorage()` monolith
- Clean navigation and provider tree structure

### Critical Issues (App-breaking or Strict-mode violating)
1. **Reanimated Render-Time Write**
   - File: `src/lib/hooks/useHomeGestures.ts:37`
   - Issue: `screenHeightSV.value = screenHeight;` executes during render phase.
   - Fix: Move into `useEffect`.

2. **Hardcoded Colors (Theme System Violation)**
   - Files: `App.tsx:83-98`, `src/screens/LibraryScreen.tsx:242-268`
   - Issue: `'#000'`, `"#fff"`, `rgba(0,0,0,...)` used directly.
   - Fix: Replace with `theme.colors.background`, `theme.colors.textPrimary`, `theme.colors.overlayMedium`, etc.

3. **Expo Go Incompatible Package**
   - File: `package.json:48`
   - Issue: `react-native-compressor` requires custom native build; crashes in Expo Go if fallback fails.
   - Fix: Document requirement or move to optional deps.

### Major Issues (Code Quality / Type Safety)
4. **Type Safety Leakage (`any` types)**
   - Files: `src/screens/FeedScreen.tsx`, `src/lib/hooks/useAiQueue.ts`, `src/components/ui/CarouselSelector.tsx`, `src/components/ui/LiquidMorphIcon.tsx`, `src/screens/HomeScreen.tsx`
   - Fix: Replace with domain interfaces (`FeedItem`, `SavedNote`, `IconName`, `ViewStyle`).

5. **Unused Dependency**
   - File: `package.json:44`
   - Issue: `ollama` package is never imported.
   - Fix: Remove from dependencies.

### Minor Issues (Hygiene)
6. **Version Pinning Drift**
   - File: `package.json:56`
   - Issue: `react-native-worklets` uses `^0.7.1` instead of exact `0.7.2`.
   - Fix: Pin to `0.7.2`.

7. **Bare Catch Blocks**
   - Files: `src/lib/videoCompressor.ts`, `src/components/features/feed/FeedVideoCard.tsx`
   - Fix: Add structured logging context.
